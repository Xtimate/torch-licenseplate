services:
  api:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped

  ui:
    build: ./spotter-ui
    ports:
      - "3000:3000"
    environment:
      - API_BASE=http://api:8000
    depends_on:
      - api
    restart: unless-stopped
